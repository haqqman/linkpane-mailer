{
  "name": "linkpane-mailer-sdk",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @linkpane/mailer-post build && npm run -w @linkpane/newsletter-subscribe build && npm run -w @linkpane/sdk build && npm run -w @linkpane/mailer build",
    "lint": "echo \"No lint configured yet\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "tsup": "^8.0.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}